# 求职工作台

双击 Job 文件夹里的「求职工作台.app」在浏览器打开独立工作台；也可使用「启动求职工作台.command」。本机服务在后台运行，不需要保留聊天或终端窗口。电脑重启后再次双击启动。固定入口为 http://127.0.0.1:4318/ ，仅这台电脑可访问。

- 使用本机 Codex 的 ChatGPT 登录；默认 Terra / Medium，不使用 API key。
- 在「我的资料」维护事实与主简历文字。原始求职档案不被覆盖。
- 输入公司官网与计划投递数，点击开始筛选；每次只运行一个任务，可停止。
- 在推荐岗位上点击定制简历，生成并下载 Markdown 文字草稿。
- 目前不输出经过一页验证的 Word/PDF，不自动投递，不保证所有招聘页面或小红书内容可访问。来源缺失必须在报告中说明。
- 资料、任务、结果保存在 .local/，关闭网页后仍保留。服务关闭会停止正在执行的任务，下次启动标记为中断。
- 当前版本不会迁移已有聊天记录，Codex 使用临时执行会话；结果由工作台单独保存。

## 维护

网页源代码为 app/；本机连接服务为 local/server.mjs。修改网页后运行 package.json 的 build 脚本，生成 dist/client/。本机服务必须在操作系统中直接运行，不能部署为 Cloudflare Worker；远程网页无法代替本机登录服务。
